# Monorepo: hoisting para que tsc, ESLint y otras tools resuelvan @scoped deps sin depender
# solo de symlinks anidados en .pnpm (problema típico en CI con pnpm 8+ y workspace).
# Ver: https://pnpm.io/npmrc#shamefully-hoist
shamefully-hoist=true

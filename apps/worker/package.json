{
  "name": "@bot-dca/worker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "echo 'TECH-001 placeholder build worker'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "format": "prettier --write \"src/**/*.ts\""
  }
}

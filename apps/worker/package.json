{
  "name": "@bot-dca/worker",
  "private": true,
  "scripts": {
    "dev": "pnpm exec tsx watch src/main.ts",
    "build": "pnpm exec tsc -p tsconfig.build.json",
    "test": "node -e \"console.log('worker test placeholder')\"",
    "lint": "node -e \"console.log('worker lint delegated to root eslint')\"",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
